# Fase 0.6 — Calidad y cierre · Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Auditar la canónica, parchar lo trivial, medir cobertura DANE, emitir documento de cierre que autoriza Fase 1.

**Architecture:** Cero rediseño. Scripts SQL/TS instrumentales que leen `raw_record` y la canónica; salida humana (psql / stdout). Verificación manual en Postgres local — no hay test unitarios para SQL puro de auditoría. El doc de cierre integra resultados.

**Tech Stack:** Postgres 17 local, Drizzle (solo lectura aquí), `tsx`, `pg` driver. La canónica ya existe; el plan no toca esquema.

**Spec:** [docs/superpowers/specs/2026-06-06-fase-0.6-cierre-design.md](docs/superpowers/specs/2026-06-06-fase-0.6-cierre-design.md)

**Prerrequisitos antes de empezar:**
- Postgres local con `DATABASE_URL` y `DB_DRIVER=node` exportados.
- Migraciones aplicadas (`npm run db:migrate`).
- Seed DANE corrido (`npm run db:seed-geografia`).
- Muestra cargada (`npm run db:load-sample`).
- Canónica reconstruida una vez (`npm run db:transform`).

Si algo falla, **parar** y reportar al usuario antes de continuar.

---

## File structure

| Archivo | Tipo | Tareas que lo tocan |
|---|---|---|
| `scripts/sql/quality-checks-0.6.sql` | nuevo | T1 |
| `scripts/audit-proveedores.ts` | nuevo | T2 |
| `data/dane/divipola.ts` | extender | T3 |
| `src/lib/transform/normalize.ts` | parchar si T2 lo justifica | T2 |
| `src/lib/transform/nit.ts` | parchar si T2 lo justifica | T2 |
| `scripts/sql/preguntas-cierre-0.6.sql` | nuevo | T5 |
| `docs/fase-0/0.6-cierre-fase-0.md` | nuevo | T6 |

**No se toca:** esquema, migraciones, writers, mapCanonical, runIngest, runSweep, orchestrator.

---

## Task 0: Baseline

**Files:**
- Tocar: ninguno. Solo ejecutar y capturar.

- [ ] **Paso 1: Verificar entorno**

Run:
```bash
echo "DATABASE_URL=$DATABASE_URL  DB_DRIVER=$DB_DRIVER"
psql "$DATABASE_URL" -c "SELECT 1;"
```

Expected: la conexión funciona.

- [ ] **Paso 2: Capturar conteos baseline**

Run:
```bash
psql "$DATABASE_URL" -c "
SELECT 'raw procesos' tabla, COUNT(*) total FROM raw_record WHERE source='secop_ii_procesos'
UNION ALL SELECT 'raw contratos', COUNT(*) FROM raw_record WHERE source='secop_ii_contratos'
UNION ALL SELECT 'proveedor', COUNT(*) FROM proveedor
UNION ALL SELECT 'entidad', COUNT(*) FROM entidad
UNION ALL SELECT 'geografia', COUNT(*) FROM geografia
UNION ALL SELECT 'geografia_alias', COUNT(*) FROM geografia_alias
UNION ALL SELECT 'proceso', COUNT(*) FROM proceso
UNION ALL SELECT 'contrato', COUNT(*) FROM contrato
UNION ALL SELECT 'contrato_evento', COUNT(*) FROM contrato_evento
UNION ALL SELECT 'transform_quarantine', COUNT(*) FROM transform_quarantine
UNION ALL SELECT 'sync_log', COUNT(*) FROM sync_log;
" | tee /tmp/baseline-0.6.txt
```

Expected: conteos coherentes con 0.5 §5.5 (proveedor ≈ 556, entidad ≈ 416, proceso ≈ 698, contrato ≈ 699, cuarentena 0). Si difieren mucho, parar y reportar.

- [ ] **Paso 3: Marcar el baseline en git**

Run:
```bash
cp /tmp/baseline-0.6.txt scripts/sql/baseline-0.6.txt
git add scripts/sql/baseline-0.6.txt
git commit -m "chore(fase-0.6): baseline de conteos antes de auditar"
```

---

## Task 1: Chequeos de calidad (E3)

**Files:**
- Create: `scripts/sql/quality-checks-0.6.sql`

- [ ] **Paso 1: Crear el script SQL**

Crear `scripts/sql/quality-checks-0.6.sql` con este contenido exacto:

```sql
-- Fase 0.6 — Chequeos de calidad
-- 5 secciones: C1 huérfanos, C2 nulos críticos, C3 rangos, C4/C5 protocolo externo.
-- Uso: psql "$DATABASE_URL" -f scripts/sql/quality-checks-0.6.sql

\echo '====================================================================='
\echo 'C1 — Huérfanos'
\echo '====================================================================='

\echo '-- C1.1 contratos sin proceso (EXENTO masivo: artefacto BDOS, 0.2 §5.1)'
SELECT COUNT(*) FILTER (WHERE proceso_id IS NULL) AS contratos_sin_proceso_total,
       COUNT(*) AS contratos_total
FROM contrato;

\echo '-- C1.2 procesos sin entidad (NO debería pasar — entidad es FK obligatoria)'
SELECT COUNT(*) AS procesos_sin_entidad FROM proceso WHERE entidad_id IS NULL;

\echo '-- C1.3 contratos sin entidad (NO debería pasar)'
SELECT COUNT(*) AS contratos_sin_entidad FROM contrato WHERE entidad_id IS NULL;

\echo '-- C1.4 raw que NO mapeó a canónica (latest snapshot por source_record_id)'
WITH latest_procesos AS (
  SELECT DISTINCT ON (source_record_id) id, source_record_id
  FROM raw_record WHERE source='secop_ii_procesos'
  ORDER BY source_record_id, ingested_at DESC
),
latest_contratos AS (
  SELECT DISTINCT ON (source_record_id) id, source_record_id
  FROM raw_record WHERE source='secop_ii_contratos'
  ORDER BY source_record_id, ingested_at DESC
)
SELECT
  (SELECT COUNT(*) FROM latest_procesos lp
    WHERE NOT EXISTS (SELECT 1 FROM proceso p WHERE p.secop_proceso_id = lp.source_record_id))
    AS procesos_raw_sin_canonica,
  (SELECT COUNT(*) FROM latest_contratos lc
    WHERE NOT EXISTS (SELECT 1 FROM contrato c WHERE c.secop_contrato_id = lc.source_record_id))
    AS contratos_raw_sin_canonica;

\echo '====================================================================='
\echo 'C2 — Nulos críticos'
\echo '====================================================================='

\echo '-- C2.1 proveedor: nit_canonico nunca debería ser NULL (es la UNIQUE)'
SELECT COUNT(*) AS proveedor_sin_nit FROM proveedor WHERE nit_canonico IS NULL;

\echo '-- C2.2 contrato: campos obligatorios'
SELECT
  COUNT(*) FILTER (WHERE secop_contrato_id IS NULL) AS sin_secop_id,
  COUNT(*) FILTER (WHERE valor_inicial IS NULL)     AS sin_valor_inicial,
  COUNT(*) FILTER (WHERE estado_actual IS NULL)     AS sin_estado_actual,
  COUNT(*) FILTER (WHERE fecha_inicio IS NULL)      AS sin_fecha_inicio,
  COUNT(*)                                          AS total
FROM contrato;

\echo '-- C2.3 proceso: campos obligatorios'
SELECT
  COUNT(*) FILTER (WHERE secop_proceso_id IS NULL) AS sin_secop_id,
  COUNT(*) FILTER (WHERE objeto IS NULL)           AS sin_objeto,
  COUNT(*) FILTER (WHERE estado_actual IS NULL)    AS sin_estado_actual,
  COUNT(*)                                         AS total
FROM proceso;

\echo '====================================================================='
\echo 'C3 — Rangos'
\echo '====================================================================='

\echo '-- C3.1 valor_inicial <= 0 (sospechoso; 0 puede ser válido pero raro)'
SELECT COUNT(*) FILTER (WHERE valor_inicial = 0)  AS valor_inicial_cero,
       COUNT(*) FILTER (WHERE valor_inicial < 0)  AS valor_inicial_negativo
FROM contrato;

\echo '-- C3.2 valor_actual < valor_inicial (no debería pasar: adiciones suben)'
SELECT COUNT(*) AS valor_actual_menor_que_inicial
FROM contrato WHERE valor_actual < valor_inicial;

\echo '-- C3.3 fecha_fin_actual < fecha_inicio'
SELECT COUNT(*) AS fecha_fin_antes_que_inicio
FROM contrato WHERE fecha_fin_actual < fecha_inicio;

\echo '-- C3.4 ingested_at futuro (reloj mal)'
SELECT COUNT(*) AS ingested_futuro FROM raw_record WHERE ingested_at > NOW();

\echo '====================================================================='
\echo 'Cobertura geográfica (informativo, alimenta T3)'
\echo '====================================================================='

\echo '-- Cobertura por tabla'
SELECT 'proceso' tabla,
       COUNT(*) FILTER (WHERE geografia_id IS NOT NULL) AS con_geo,
       COUNT(*) AS total,
       ROUND(100.0 * COUNT(*) FILTER (WHERE geografia_id IS NOT NULL) / NULLIF(COUNT(*),0), 1) AS pct
FROM proceso
UNION ALL
SELECT 'contrato',
       COUNT(*) FILTER (WHERE geografia_id IS NOT NULL),
       COUNT(*),
       ROUND(100.0 * COUNT(*) FILTER (WHERE geografia_id IS NOT NULL) / NULLIF(COUNT(*),0), 1)
FROM contrato;

\echo '-- Top 30 textos crudos NO resueltos (contratos): entrada para extender DANE en T3'
SELECT payload->>'localizaci_n' AS localizacion_raw, COUNT(*) AS n
FROM raw_record r
JOIN contrato c ON c.secop_contrato_id = r.source_record_id
WHERE r.source='secop_ii_contratos'
  AND r.id = c.raw_record_id_actual
  AND c.geografia_id IS NULL
  AND payload->>'localizaci_n' IS NOT NULL
GROUP BY payload->>'localizaci_n'
ORDER BY n DESC
LIMIT 30;
```

- [ ] **Paso 2: Ejecutar y capturar**

Run:
```bash
psql "$DATABASE_URL" -f scripts/sql/quality-checks-0.6.sql 2>&1 | tee /tmp/quality-checks-0.6.txt
```

Expected:
- C1.1 contratos_sin_proceso_total ≈ 699 (EXENTO documentado).
- C1.2 / C1.3 deben ser 0. Si no, **parar y reportar**.
- C1.4 procesos_raw_sin_canonica = 0 y contratos_raw_sin_canonica = 0. Si no, indica fallo del transform.
- C2.1 = 0. Si no, **parar**.
- C2.2 / C2.3: capturar valores. Cualquier nulo crítico distinto a `valor_inicial` o `estado_actual` se documenta como sospecha.
- C3.x: capturar. Cualquier valor > 0 se documenta o se investiga.
- Cobertura: capturar % y top-30 no-resueltos. Es la entrada de T3.

- [ ] **Paso 3: Inspeccionar y decidir**

Mira `/tmp/quality-checks-0.6.txt`. Si alguno de los chequeos NO-EXENTOS revela algo que sugiera defecto estructural del transform (e.g. `valor_inicial NULL` en mayoría de contratos, `fecha_fin < fecha_inicio` en >5%), **parar y reportar al usuario**. No parchar `mapCanonical.ts` sin aprobación.

Si todo está dentro de lo esperado, seguir.

- [ ] **Paso 4: Commit**

```bash
cp /tmp/quality-checks-0.6.txt scripts/sql/quality-checks-0.6.output.txt
git add scripts/sql/quality-checks-0.6.sql scripts/sql/quality-checks-0.6.output.txt
git commit -m "feat(fase-0.6): script de chequeos de calidad + salida baseline"
```

---

## Task 2: Auditoría de proveedores (E1)

**Files:**
- Create: `scripts/audit-proveedores.ts`

- [ ] **Paso 1: Crear el script**

Crear `scripts/audit-proveedores.ts` con este contenido:

```typescript
/**
 * Fase 0.6 — Auditoría de proveedores (E1).
 * 3 reportes: R1.1 razones sociales múltiples, R1.2 NIT basura nueva,
 * R1.3 tipo_documento mal asignado.
 *
 * Uso: tsx scripts/audit-proveedores.ts
 */
import { Pool } from 'pg';

const pool = new Pool({ connectionString: process.env.DATABASE_URL });

async function r11_razonesMultiples() {
  console.log('\n=== R1.1 — NITs con razones sociales múltiples ===');
  // proveedor es UNIQUE por nit_canonico → si hay >1 razón social vemos
  // colisiones en raw_record. Las miramos vía payload del último snapshot.
  const { rows } = await pool.query(`
    WITH razones_por_nit AS (
      SELECT
        regexp_replace(coalesce(payload->>'documento_proveedor',''), '\\D', '', 'g') AS nit_raw,
        payload->>'proveedor_adjudicado' AS razon
      FROM raw_record
      WHERE source = 'secop_ii_contratos'
        AND payload->>'documento_proveedor' IS NOT NULL
        AND payload->>'proveedor_adjudicado' IS NOT NULL
    ),
    agrupados AS (
      SELECT nit_raw, COUNT(DISTINCT razon) AS n_razones,
             array_agg(DISTINCT razon) AS razones
      FROM razones_por_nit
      WHERE length(nit_raw) >= 5
      GROUP BY nit_raw
      HAVING COUNT(DISTINCT razon) > 1
    )
    SELECT COUNT(*) AS total_nits_con_multiples FROM agrupados;
  `);
  console.log(`  total NITs con >1 razón social: ${rows[0].total_nits_con_multiples}`);

  const top = await pool.query(`
    WITH razones_por_nit AS (
      SELECT
        regexp_replace(coalesce(payload->>'documento_proveedor',''), '\\D', '', 'g') AS nit_raw,
        payload->>'proveedor_adjudicado' AS razon
      FROM raw_record
      WHERE source = 'secop_ii_contratos'
        AND payload->>'documento_proveedor' IS NOT NULL
        AND payload->>'proveedor_adjudicado' IS NOT NULL
    )
    SELECT nit_raw, COUNT(DISTINCT razon) AS n_razones,
           (array_agg(DISTINCT razon))[1:3] AS muestra
    FROM razones_por_nit
    WHERE length(nit_raw) >= 5
    GROUP BY nit_raw
    HAVING COUNT(DISTINCT razon) > 1
    ORDER BY n_razones DESC
    LIMIT 10;
  `);
  console.log('  top 10:');
  for (const row of top.rows) {
    console.log(`    ${row.nit_raw} (${row.n_razones} razones): ${row.muestra.join(' | ')}`);
  }
}

async function r12_nitBasura() {
  console.log('\n=== R1.2 — Patrones de NIT basura no contemplados ===');
  // Los actuales contemplados: regex de centinelas en normalize.ts +
  // el chequeo "documento sin dígitos". Buscamos textos que entraron como
  // proveedor_raw (proveedor_id IS NULL) y agrupamos.
  const { rows } = await pool.query(`
    SELECT proveedor_raw, COUNT(*) AS n
    FROM contrato
    WHERE proveedor_id IS NULL AND proveedor_raw IS NOT NULL
    GROUP BY proveedor_raw
    ORDER BY n DESC
    LIMIT 20;
  `);
  if (!rows.length) {
    console.log('  (sin filas con proveedor_id NULL — el normalizador resolvió todo)');
    return;
  }
  console.log('  patrones que terminaron en proveedor_raw:');
  for (const row of rows) {
    console.log(`    [${row.n}] ${row.proveedor_raw}`);
  }
  console.log('  → si hay patrones que NO son centinela legítimo, ampliar normalize.ts SENTINELS');
}

async function r13_tipoDocMal() {
  console.log('\n=== R1.3 — tipo_documento sospechoso ===');
  // Caso 1: tipo_documento = NIT pero el número tiene <= 7 dígitos (más típico de CC)
  const cc = await pool.query(`
    SELECT COUNT(*) AS n_nit_corto FROM proveedor
    WHERE tipo_documento='NIT' AND length(nit_canonico) <= 7;
  `);
  console.log(`  proveedores con tipo_documento='NIT' y length(nit_canonico)<=7: ${cc.rows[0].n_nit_corto}`);

  // Caso 2: tipo_documento != NIT pero el número tiene >= 9 dígitos (típico NIT empresa)
  const nit = await pool.query(`
    SELECT tipo_documento, COUNT(*) AS n_largo
    FROM proveedor
    WHERE tipo_documento <> 'NIT' AND length(nit_canonico) >= 9
    GROUP BY tipo_documento;
  `);
  console.log('  proveedores con tipo_documento != NIT y length(nit_canonico)>=9:');
  for (const row of nit.rows) {
    console.log(`    ${row.tipo_documento}: ${row.n_largo}`);
  }
  console.log('  → si los conteos son significativos, revisar normalizeTipoDoc en nit.ts');
}

async function main() {
  await r11_razonesMultiples();
  await r12_nitBasura();
  await r13_tipoDocMal();
  await pool.end();
}

main().catch((e) => {
  console.error('audit-proveedores falló:', e);
  pool.end().catch(() => {}).finally(() => process.exit(1));
});
```

- [ ] **Paso 2: Añadir script en package.json**

Modificar `package.json` en la sección `scripts` para añadir (en el orden lógico, junto a los otros `db:`):

```json
"db:audit-proveedores": "tsx scripts/audit-proveedores.ts"
```

- [ ] **Paso 3: Ejecutar y capturar**

Run:
```bash
npm run db:audit-proveedores 2>&1 | tee /tmp/audit-proveedores-0.6.txt
```

Expected: 3 secciones impresas. Anotar:
- R1.1: número de NITs con razones múltiples (sin acción, va a deuda).
- R1.2: si hay patrones no contemplados que sean **claramente centinela** (e.g. `"NO REGISTRA"`, `"PENDIENTE"`), agregar al `SENTINELS` de [src/lib/transform/normalize.ts](src/lib/transform/normalize.ts:17). Si son nombres de personas legítimas, NO tocar.
- R1.3: si los conteos son altos (>5% del total de proveedores), revisar `normalizeTipoDoc` en [src/lib/transform/nit.ts](src/lib/transform/nit.ts:39). Si son bajos, va a deuda.

- [ ] **Paso 4: Aplicar parche (solo si R1.2 o R1.3 lo justifican)**

Si R1.2 reveló centinelas nuevos, editar `src/lib/transform/normalize.ts` línea 17 (el `SENTINELS` Set) y añadir las versiones lowercase sin acentos. Por ejemplo:

```typescript
const SENTINELS = new Set([
  'no definido',
  'no definida',
  'sin definir',
  'por definir',
  'no adjudicado',
  'sin descripcion',
  'no aplica',
  'n/a',
  'na',
  // T2 R1.2 (2026-06-06): añadidos tras auditoría
  'no registra',
  'pendiente',
]);
```

Si NO hay centinelas nuevos, saltar a Paso 6.

- [ ] **Paso 5: Re-correr transform y vitest**

Run:
```bash
npm test
npm run db:transform 2>&1 | tee /tmp/transform-post-audit.txt
```

Expected: tests 127+ verdes (no rompimos nada); transform sin errores; conteo de proveedores con `proveedor_id NULL` baja si añadimos centinelas.

- [ ] **Paso 6: Commit**

```bash
cp /tmp/audit-proveedores-0.6.txt scripts/audit-proveedores-0.6.output.txt
git add scripts/audit-proveedores.ts scripts/audit-proveedores-0.6.output.txt package.json
# si hubo parche:
git add src/lib/transform/normalize.ts 2>/dev/null
git commit -m "feat(fase-0.6): auditoría de proveedores + parche centinelas si aplica"
```

---

## Task 3: Extensión del crosswalk DANE (E2)

**Files:**
- Modify: `data/dane/divipola.ts`

- [ ] **Paso 1: Recuperar el top de no-resueltos**

Del output de T1 (`scripts/sql/quality-checks-0.6.output.txt`), extraer el bloque "Top 30 textos crudos NO resueltos". Identificar los que NO son centinela genuino:
- Centinela genuino (saltar): `Colombia, No Definido, No Definido`, `No Definido`, vacío.
- Real (atender): `Colombia, Antioquia, Yarumal`, `Colombia, Cundinamarca, Cota`, etc.

Si todos son centinela, T3 termina aquí — saltar a Paso 4 y registrar "no hubo no-resueltos reales" en el reporte.

- [ ] **Paso 2: Verificar códigos DIVIPOLA**

Para cada texto real, buscar el código DIVIPOLA oficial. Fuente: <https://www.dane.gov.co/> o `data/dane/divipola.ts` (estructura existente). Si un código no se puede verificar con confianza, **omitirlo** y anotarlo como deuda.

- [ ] **Paso 3: Extender `data/dane/divipola.ts`**

Leer la estructura actual del archivo:

```bash
head -50 data/dane/divipola.ts
tail -30 data/dane/divipola.ts
```

Añadir las entradas nuevas siguiendo el formato existente (municipios + aliases si el texto observado difiere del nombre canónico). Mantener orden alfabético dentro de cada departamento.

- [ ] **Paso 4: Re-correr seed + transform**

Run:
```bash
npm run db:seed-geografia 2>&1 | tee /tmp/seed-post-t3.txt
npm run db:transform 2>&1 | tee /tmp/transform-post-t3.txt
```

Expected: el `seed-geografia` reporta las nuevas entradas; el `transform` muestra que `geografía resuelta` subió.

- [ ] **Paso 5: Medir cobertura post-T3**

Run:
```bash
psql "$DATABASE_URL" -c "
SELECT 'proceso' tabla,
       COUNT(*) FILTER (WHERE geografia_id IS NOT NULL) AS con_geo,
       COUNT(*) AS total,
       ROUND(100.0 * COUNT(*) FILTER (WHERE geografia_id IS NOT NULL) / NULLIF(COUNT(*),0), 1) AS pct
FROM proceso
UNION ALL
SELECT 'contrato',
       COUNT(*) FILTER (WHERE geografia_id IS NOT NULL),
       COUNT(*),
       ROUND(100.0 * COUNT(*) FILTER (WHERE geografia_id IS NOT NULL) / NULLIF(COUNT(*),0), 1)
FROM contrato;
" | tee /tmp/cobertura-post-t3.txt
```

Expected: cobertura sube respecto al baseline de T1. Anotar el delta para el doc de cierre.

- [ ] **Paso 6: Commit**

```bash
git add data/dane/divipola.ts /tmp/cobertura-post-t3.txt
cp /tmp/cobertura-post-t3.txt scripts/sql/cobertura-post-t3.txt
git add scripts/sql/cobertura-post-t3.txt
git commit -m "feat(fase-0.6): extensión crosswalk DANE + delta cobertura"
```

---

## Task 4: Idempotencia + reproceso completo (C4 + C5)

**Files:**
- Tocar: ninguno. Solo ejecutar.

- [ ] **Paso 1: Capturar snapshot pre-idempotencia**

Run:
```bash
psql "$DATABASE_URL" -c "
SELECT 'proveedor' t, COUNT(*) n FROM proveedor
UNION ALL SELECT 'entidad', COUNT(*) FROM entidad
UNION ALL SELECT 'proceso', COUNT(*) FROM proceso
UNION ALL SELECT 'contrato', COUNT(*) FROM contrato
UNION ALL SELECT 'transform_quarantine', COUNT(*) FROM transform_quarantine;
" | tee /tmp/snapshot-pre-c4.txt
```

- [ ] **Paso 2: C4 — idempotencia (re-correr transform)**

Run:
```bash
npm run db:transform 2>&1 | tee /tmp/c4-run.txt
psql "$DATABASE_URL" -c "
SELECT 'proveedor' t, COUNT(*) n FROM proveedor
UNION ALL SELECT 'entidad', COUNT(*) FROM entidad
UNION ALL SELECT 'proceso', COUNT(*) FROM proceso
UNION ALL SELECT 'contrato', COUNT(*) FROM contrato
UNION ALL SELECT 'transform_quarantine', COUNT(*) FROM transform_quarantine;
" | tee /tmp/snapshot-post-c4.txt
diff /tmp/snapshot-pre-c4.txt /tmp/snapshot-post-c4.txt
```

Expected: `diff` vacío. Si `transform_quarantine` cambió (es append-only), está OK porque añade un registro por intento — pero el resto debe ser idéntico. Si proveedor/entidad/proceso/contrato cambian, **parar y reportar**.

- [ ] **Paso 3: C5 — reproceso completo desde raw_record**

Run:
```bash
psql "$DATABASE_URL" -c "
TRUNCATE contrato_evento, contrato, proceso CASCADE;
TRUNCATE proveedor CASCADE;
TRUNCATE entidad CASCADE;
"
# NO truncar: geografia, geografia_alias (esos son seed)
# NO truncar: raw_record, sync_log, transform_quarantine

npm run db:transform 2>&1 | tee /tmp/c5-run.txt

psql "$DATABASE_URL" -c "
SELECT 'proveedor' t, COUNT(*) n FROM proveedor
UNION ALL SELECT 'entidad', COUNT(*) FROM entidad
UNION ALL SELECT 'proceso', COUNT(*) FROM proceso
UNION ALL SELECT 'contrato', COUNT(*) FROM contrato;
" | tee /tmp/snapshot-post-c5.txt
```

Expected: conteos de `proveedor`, `entidad`, `proceso`, `contrato` **idénticos** a `/tmp/snapshot-post-c4.txt`. Si difieren, **parar y reportar** — significa que el transform no es 100% reconstruible desde raw.

- [ ] **Paso 4: Commit la evidencia**

```bash
cp /tmp/snapshot-pre-c4.txt scripts/sql/c4-pre.txt
cp /tmp/snapshot-post-c4.txt scripts/sql/c4-post.txt
cp /tmp/snapshot-post-c5.txt scripts/sql/c5-post.txt
git add scripts/sql/c4-pre.txt scripts/sql/c4-post.txt scripts/sql/c5-post.txt
git commit -m "feat(fase-0.6): evidencia C4 idempotencia + C5 reproceso completo"
```

---

## Task 5: Tres queries SQL de cierre (E4)

**Files:**
- Create: `scripts/sql/preguntas-cierre-0.6.sql`

- [ ] **Paso 1: Descubrir los valores de `estado_actual` activos**

Run:
```bash
psql "$DATABASE_URL" -c "
SELECT estado_actual, COUNT(*) FROM contrato GROUP BY estado_actual ORDER BY 2 DESC;
"
```

Anotar los valores. Los que indiquen "en ejecución / vigente / activo" son los que usa la query (b).

- [ ] **Paso 2: Crear el script SQL**

Crear `scripts/sql/preguntas-cierre-0.6.sql` con este contenido. **AJUSTAR** la lista del `IN (...)` de la query (b) con los valores descubiertos en el Paso 1:

```sql
-- Fase 0.6 — Tres preguntas SQL de cierre.
-- Demuestran que la canónica responde sin tocar raw_record.

\echo '====================================================================='
\echo '(a) Top 5 proveedores por valor adjudicado'
\echo '====================================================================='
SELECT
  p.nit_canonico,
  p.razon_social,
  COUNT(c.id) AS nro_contratos,
  SUM(c.valor_actual) AS valor_total
FROM contrato c
JOIN proveedor p ON p.id = c.proveedor_id
GROUP BY p.id, p.nit_canonico, p.razon_social
ORDER BY valor_total DESC
LIMIT 5;

\echo '====================================================================='
\echo '(b) Contratos activos por municipio'
\echo '====================================================================='
-- AJUSTAR los valores tras el Paso 1 (e.g. 'En ejecución', 'Activo', ...)
SELECT
  g.municipio_nombre,
  g.departamento_nombre,
  COUNT(*) AS contratos_activos
FROM contrato c
LEFT JOIN geografia g ON g.codigo_divipola = c.geografia_id
WHERE c.estado_actual IN ('En ejecución', 'Activo')  -- AJUSTAR
GROUP BY g.municipio_nombre, g.departamento_nombre
ORDER BY contratos_activos DESC;

\echo '-- contratos activos SIN geografía (informativo, no falla la pregunta)'
SELECT COUNT(*) AS activos_sin_geo
FROM contrato
WHERE estado_actual IN ('En ejecución', 'Activo')  -- AJUSTAR
  AND geografia_id IS NULL;

\echo '====================================================================='
\echo '(c) Entidades que adjudicaron >=2 contratos al mismo proveedor'
\echo '====================================================================='
SELECT
  e.nombre AS entidad,
  p.razon_social AS proveedor,
  COUNT(*) AS nro_contratos,
  SUM(c.valor_actual) AS valor_total
FROM contrato c
JOIN entidad e   ON e.id = c.entidad_id
JOIN proveedor p ON p.id = c.proveedor_id
GROUP BY e.id, e.nombre, p.id, p.razon_social
HAVING COUNT(*) >= 2
ORDER BY nro_contratos DESC, valor_total DESC
LIMIT 20;
```

- [ ] **Paso 3: Ejecutar y capturar**

Run:
```bash
psql "$DATABASE_URL" -f scripts/sql/preguntas-cierre-0.6.sql 2>&1 | tee /tmp/preguntas-cierre-0.6.txt
```

Expected: las 3 queries devuelven resultado coherente. Si (b) está vacía porque ningún contrato tiene estado activo en la muestra, anotar para el doc de cierre.

- [ ] **Paso 4: Commit**

```bash
cp /tmp/preguntas-cierre-0.6.txt scripts/sql/preguntas-cierre-0.6.output.txt
git add scripts/sql/preguntas-cierre-0.6.sql scripts/sql/preguntas-cierre-0.6.output.txt
git commit -m "feat(fase-0.6): 3 queries SQL de cierre + salida"
```

---

## Task 6: Documento de cierre (E5 + E6)

**Files:**
- Create: `docs/fase-0/0.6-cierre-fase-0.md`

- [ ] **Paso 1: Recopilar evidencia**

Tener a mano:
- `scripts/sql/baseline-0.6.txt`
- `scripts/sql/quality-checks-0.6.output.txt`
- `scripts/audit-proveedores-0.6.output.txt`
- `scripts/sql/cobertura-post-t3.txt`
- `scripts/sql/c4-pre.txt`, `scripts/sql/c4-post.txt`, `scripts/sql/c5-post.txt`
- `scripts/sql/preguntas-cierre-0.6.output.txt`

- [ ] **Paso 2: Escribir `docs/fase-0/0.6-cierre-fase-0.md`**

Estructura exacta:

```markdown
# Fase 0.6 — Calidad y cierre de Fase 0

> Documento de cierre. Última pieza de Fase 0 (cimientos). Autoriza (o no) el paso a Fase 1.

## 0. Resumen ejecutivo

Una frase: qué se hizo en 0.6. Tres frases: qué se midió, qué se corrigió, qué quedó como deuda.

## 1. Chequeos de calidad (E3)

Tabla con los 5 checks (C1–C5), nº de anomalías, qué se hizo con cada una.

(Pegar los conteos relevantes de quality-checks-0.6.output.txt + c4-* + c5-post.)

## 2. Auditoría de proveedores (E1)

Tres reportes:
- R1.1 NITs con razones múltiples: nº + top-3 ejemplos.
- R1.2 Patrones nuevos de centinela: lista + parche aplicado (link al commit).
- R1.3 tipo_documento sospechoso: conteos.

## 3. Cobertura DANE (E2)

Antes / después. Lista corta de municipios añadidos. Pendientes que quedan (deuda).

## 4. Tres preguntas SQL (E4)

Para cada una: query usada (link al .sql) + top filas + lectura.

## 5. Deuda técnica (E5)

| ID | Descripción | Origen | Impacto | Fase futura |
|---|---|---|---|---|
| D-001 | Crosswalk DANE incompleto (X municipios pendientes) | 0.6 | bajo | on-demand |
| D-002 | proveedor_alias (mismo NIT, razones distintas) | 0.6 | medio | cuando entre RUES |
| D-003 | Colisiones de NIT temporal en consorcios | 0.6 | bajo | con muestra mayor |
| D-004 | Parser de localizaci_n con comas en nombre depto | 0.4 | bajo | Fase 1 |
| D-005 | proceso.estado_codigo text vs numeric | 0.2.2 / 0.4 | bajo | cuando query lo justifique |
| D-006 | proveedor_miembro vacía | 0.1 | medio | con RUP/RUES |
| D-007 | Detección de eventos (contrato_evento) | 0.1 | alto | Fase 1 |
| D-008 | Optimización transform por batch_id | 0.5 | bajo | cuando duela |
| D-009 | Cron de ingesta automática | 0.5 | medio | fuera de Fase 0 |

(añadir cualquier ítem que surgió durante 0.6).

## 6. Veredicto

Responde cada pregunta sin rodeos:

### 6.1 ¿El modelo aguanta adiciones y prórrogas como eventos sin rediseño?
Respuesta directa + cita a 0.1 §6.

### 6.2 ¿Los datos crudos son reprocesables sin re-descargar la fuente?
Respuesta directa + cita a C5 (`scripts/sql/c5-post.txt`).

### 6.3 ¿La ingesta incremental es idempotente y el watermark avanza con seguridad?
Respuesta directa + cita a C4 + 0.5 §5.

### 6.4 ¿Un contrato se reconstruye correcto campo por campo desde raw_record?
Respuesta directa + cita a 0.4 §6.1.

### 6.5 ¿Dedup y DANE están resueltos o documentados como deuda?
Resumen: huecos cerrados, huecos a deuda, cobertura medida.

### 6.6 VEREDICTO FINAL

**SÍ / PARCIAL / NO.**

Justificación corta. Si PARCIAL o NO, lista exacta de bloqueos.

## 7. Cierre

Última actualización: 2026-06-06. Fase 0 cerrada. Autorización para empezar Fase 1: [SÍ | NO].
```

Rellenar cada sección con los números reales recolectados en Paso 1.

- [ ] **Paso 3: Commit final**

```bash
git add docs/fase-0/0.6-cierre-fase-0.md
git commit -m "$(cat <<'EOF'
docs(fase-0.6): documento de cierre — Fase 0 completa

Veredicto, evidencia citada, lista exhaustiva de deuda técnica.
Autoriza paso a Fase 1.
EOF
)"
```

---

## Self-review

Coverage vs spec:
- E1 → T2. ✓
- E2 → T3. ✓
- E3 → T1 (estructura) + T4 (idempotencia/reproceso). ✓
- E4 → T5. ✓
- E5 → integrado en T6 (tabla de deuda). ✓
- E6 → T6. ✓

Orden de ejecución del spec §4: chequeos primero → dedup → DANE → re-chequeos → SQL → deuda → cierre. T0→T1→T2→T3→T4→T5→T6 lo respeta.

No hay placeholders no resueltos. Los AJUSTAR del Paso 2 de T5 son intencionales y tienen el descubrimiento previo en el mismo task. Las decisiones de T2 Paso 4 ("solo si lo justifica") tienen criterio explícito en Paso 3.

Tipos consistentes: nombres de tablas y columnas (`proveedor`, `entidad`, `proceso`, `contrato`, `raw_record`, `geografia`, `nit_canonico`, `geografia_id`, `valor_inicial`, `valor_actual`, `estado_actual`, `proceso_id`, `proveedor_id`, `proveedor_raw`) coinciden con el modelo en 0.1 §4 y el código actual.

Riesgo principal del spec §7 (defectos masivos descubiertos en T1) tiene gate explícito en T1 Paso 3 y T4 Paso 2 ("parar y reportar"). No silencioso.
