# Fase 0.6 — Calidad y cierre de Fase 0

> **Estado:** plantilla. Los conteos, listas y veredicto se completan tras ejecutar localmente los scripts T0–T5 del plan `docs/superpowers/plans/2026-06-06-fase-0.6-cierre.md`.

> Documento de cierre. Última pieza de Fase 0 (cimientos). Autoriza (o no) el paso a Fase 1.

## 0. Resumen ejecutivo

(pegar resultado real — qué se hizo en 0.6, qué se midió, qué se corrigió, qué quedó como deuda)

## 1. Chequeos de calidad (E3)

Tabla con los 5 checks (C1–C5), nº de anomalías, qué se hizo con cada una.

(Pegar los conteos relevantes de quality-checks-0.6.output.txt + c4-* + c5-post.)

| Check | Sección | Resultado | Anomalías | Acción |
|-------|---------|-----------|-----------|--------|
| C1.1  | Huérfanos | contratos_sin_proceso_total | (pegar resultado real) | EXENTO documentado (0.2 §5.1) |
| C1.2  | Huérfanos | procesos_sin_entidad | (pegar resultado real) | debe ser 0 |
| C1.3  | Huérfanos | contratos_sin_entidad | (pegar resultado real) | debe ser 0 |
| C1.4  | Huérfanos | raw sin canónica | (pegar resultado real) | debe ser 0 |
| C2.1  | Nulos críticos | proveedor_sin_nit | (pegar resultado real) | debe ser 0 |
| C2.2  | Nulos críticos | contrato obligatorios | (pegar resultado real) | anotar qué nulo(s) |
| C2.3  | Nulos críticos | proceso obligatorios | (pegar resultado real) | anotar qué nulo(s) |
| C3.1  | Rangos | valor_inicial <= 0 | (pegar resultado real) | anotar si hay negativo |
| C3.2  | Rangos | valor_actual < inicial | (pegar resultado real) | debe ser 0 |
| C3.3  | Rangos | fecha_fin < inicio | (pegar resultado real) | debe ser 0 |
| C3.4  | Rangos | ingested_at futuro | (pegar resultado real) | debe ser 0 |

## 2. Auditoría de proveedores (E1)

### R1.1 NITs con razones sociales múltiples

(pegar de audit-proveedores-0.6.output.txt)

- Total NITs con >1 razón social: (pegar número)
- Top 3 ejemplos:
  - (pegar ejemplo 1)
  - (pegar ejemplo 2)
  - (pegar ejemplo 3)

Acción: anotado como deuda (D-002). No bloqueador.

### R1.2 Patrones nuevos de centinela

(pegar de audit-proveedores-0.6.output.txt)

Patrones encontrados:
- (lista de patrones si hay)

Parche aplicado: [SÍ | NO] — (link a commit si aplica)

### R1.3 tipo_documento sospechoso

(pegar de audit-proveedores-0.6.output.txt)

- NIT corto (<=7 dígitos): (número)
- Tipo !=NIT pero largo (>=9 dígitos): (detallar por tipo)

Acción: (revisar o documentar como deuda)

## 3. Cobertura DANE (E2)

### Cobertura antes (baseline)

(pegar de quality-checks-0.6.output.txt)

| Tabla | Con geo | Total | % |
|-------|---------|-------|---|
| proceso | (pegar) | (pegar) | (pegar) |
| contrato | (pegar) | (pegar) | (pegar) |

### Cobertura después

(pegar de cobertura-post-t3.txt tras T3)

| Tabla | Con geo | Total | % |
|-------|---------|-------|---|
| proceso | (pegar) | (pegar) | (pegar) |
| contrato | (pegar) | (pegar) | (pegar) |

**Delta:** (calcular % ganado)

### Top-30 no-resueltos (entrada para T3)

(pegar de quality-checks-0.6.output.txt — solo si quedan sin resolver tras T3)

Municipios añadidos en T3:
- (lista)

Pendientes como deuda:
- (lista — si T3 no cubría todos)

## 4. Tres preguntas SQL (E4)

Script: `scripts/sql/preguntas-cierre-0.6.sql` (ejecutable post-T5)

### (a) Top 5 proveedores por valor adjudicado

Query:
```sql
SELECT p.nit_canonico, p.razon_social, COUNT(c.id) AS nro_contratos, SUM(c.valor_actual) AS valor_total
FROM contrato c
JOIN proveedor p ON p.id = c.proveedor_id
GROUP BY p.id, p.nit_canonico, p.razon_social
ORDER BY valor_total DESC
LIMIT 5;
```

Resultado (pegar top 5 de preguntas-cierre-0.6.output.txt):

| NIT | Razón Social | Nº Contratos | Valor Total |
|-----|--------------|--------------|-------------|
| (pegar) | (pegar) | (pegar) | (pegar) |

Lectura: (resumir hallazgo — concentración, diversidad, etc.)

### (b) Contratos activos por municipio

Query:
```sql
SELECT g.municipio_nombre, g.departamento_nombre, COUNT(*) AS contratos_activos
FROM contrato c
LEFT JOIN geografia g ON g.codigo_divipola = c.geografia_id
WHERE c.estado_actual IN ('En ejecución', 'Activo')  -- AJUSTADO en T5 Paso 1
GROUP BY g.municipio_nombre, g.departamento_nombre
ORDER BY contratos_activos DESC;
```

Resultado (pegar top de preguntas-cierre-0.6.output.txt):

| Municipio | Departamento | Contratos Activos |
|-----------|--------------|-------------------|
| (pegar) | (pegar) | (pegar) |

Activos sin geografía: (número)

Lectura: (cobertura geográfica, dispersión, concentración)

### (c) Entidades que adjudicaron >=2 contratos al mismo proveedor

Query:
```sql
SELECT e.nombre, p.razon_social, COUNT(*) AS nro_contratos, SUM(c.valor_actual) AS valor_total
FROM contrato c
JOIN entidad e ON e.id = c.entidad_id
JOIN proveedor p ON p.id = c.proveedor_id
GROUP BY e.id, e.nombre, p.id, p.razon_social
HAVING COUNT(*) >= 2
ORDER BY nro_contratos DESC, valor_total DESC
LIMIT 20;
```

Resultado (pegar top 10 de preguntas-cierre-0.6.output.txt):

| Entidad | Proveedor | Nº Contratos | Valor Total |
|---------|-----------|--------------|-------------|
| (pegar) | (pegar) | (pegar) | (pegar) |

Lectura: (relaciones entidad–proveedor recurrentes, posibles alianzas o concentración)

## 5. Deuda técnica (E5)

| ID | Descripción | Origen | Impacto | Fase futura |
|---|---|---|---|---|
| D-001 | Crosswalk DANE incompleto (X municipios pendientes) | 0.6 | bajo | on-demand |
| D-002 | proveedor_alias (mismo NIT, razones distintas) | 0.6 | medio | cuando entre RUES |
| D-003 | Colisiones de NIT temporal en consorcios | 0.6 | bajo | con muestra mayor |
| D-004 | Parser de localizaci_n con comas en nombre depto | 0.4 | bajo | Fase 1 |
| D-005 | proceso.estado_codigo text vs numeric | 0.2.2 / 0.4 | bajo | cuando query lo justifique |
| D-006 | proveedor_miembro vacía | 0.1 | medio | con RUP/RUES |
| D-007 | Detección de eventos (contrato_evento) | 0.1 | alto | Fase 1 |
| D-008 | Optimización transform por batch_id | 0.5 | bajo | cuando duela |
| D-009 | Cron de ingesta automática | 0.5 | medio | fuera de Fase 0 |

(añadir cualquier ítem que surgió durante 0.6)

## 6. Veredicto

Responde cada pregunta sin rodeos:

### 6.1 ¿El modelo aguanta adiciones y prórrogas como eventos sin rediseño?

(Respuesta directa + cita a 0.1 §6)

### 6.2 ¿Los datos crudos son reprocesables sin re-descargar la fuente?

(Respuesta directa + cita a C5 de scripts/sql/c5-post.txt)

### 6.3 ¿La ingesta incremental es idempotente y el watermark avanza con seguridad?

(Respuesta directa + cita a C4 + 0.5 §5)

### 6.4 ¿Un contrato se reconstruye correcto campo por campo desde raw_record?

(Respuesta directa + cita a 0.4 §6.1)

### 6.5 ¿Dedup y DANE están resueltos o documentados como deuda?

(Resumen: huecos cerrados, huecos a deuda, cobertura medida)

### 6.6 VEREDICTO FINAL

**[SÍ | PARCIAL | NO]**

(Justificación corta. Si PARCIAL o NO, lista exacta de bloqueos)

## 7. Cierre

Última actualización: 2026-06-06. Fase 0 cerrada. Autorización para empezar Fase 1: [SÍ | NO].
