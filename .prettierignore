.next
out
dist
build
coverage
node_modules
.env
.env.local
*.md

# Generado por drizzle-kit: formatearlo aquí solo consigue que la siguiente
# migración lo reescriba con SU formato y el check vuelva a rojo.
drizzle/meta

# Copia literal de una fuente externa, con su sha256 registrado en el docstring
# de src/lib/diagnostico/cuestionario/co-apsb-v1.ts. Reformatearla rompe esa
# trazabilidad.
docs/referencia

# Geometría generada desde el MGN del DANE (ver data/geo/README.md). Va serializada
# compacta a propósito: formatearla la convierte en ~30.000 líneas de una coordenada
# cada una, con lo que cualquier actualización del MGN produce un diff ilegible.
data/geo/*.geo.json
